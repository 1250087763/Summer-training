#include<stdio.h>
int main(void)
{
    int n;
    while(scanf("%d",&n)!=EOF)
    {
        int a[10001],b[10001]={0},x,flag,flag1,max,q;
        flag=0;
        for(int i=0;i<n;i++)
        {
            scanf("%d",&x);
            flag1=0;
            for(intj=0;j<=flag;j++)
            {
                if(a[j]==x)
                {
                    b[j]++;
                    flag1=1;
                    break;
                }
            }
            if(flag1==0)
                a[flag++]=x;
        }
        max=0;
        for(int i=0;i<=flag;i++)
        {
            if(b[i]>max)
            {
                max=b[i];
                q=i;
            }
        }
        printf("%d\n",a[q]);
    }
    return 0;
}
