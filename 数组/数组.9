int max(int a, int b){
    return a > b ? a : b;
}
int numTimesAllBlue(int* light, int lightSize){
    int res = 0, max_light = 0;
    for(int i = 0;i < lightSize;i++){
        max_light = max(max_light, light[i]);
        if(i + 1 == max_light)
            res++;
    }
    return res;
}
